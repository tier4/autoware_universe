/**:
  ros__parameters:
    approximate_sync: false
    precision: fp16
    mean: [123.675, 116.28, 103.53]
    std: [58.395, 57.12, 57.375]
    over_exposure_threshold: 0.85
    under_exposure_threshold: -0.83
    backlight_threshold: 0.85
    # classifier_type: 0 = HSVFilter, 1 = CNN, 2 = CoMLOps-TLR (CoMLOps-TLR-v1.0.1-dynamic_batch)
    classifier_type: 2
    traffic_light_type: 0
    # CoMLOps-TLR (classifier_type: 2) optional parameters
    score_threshold: 0.2
    max_batch_size: 64
    input_height: 256   # CoMLOps-TLR-v1.0.1-dynamic_batch input H (fallback if ONNX dims dynamic)
    input_width: 256    # CoMLOps-TLR-v1.0.1-dynamic_batch input W (fallback if ONNX dims dynamic)
