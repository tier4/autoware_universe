/**:
  ros__parameters:
    lane_following_drivable_area:
      margin_m: 0.05 # [m] the lane bounds and road_borders are moved this far towards the inside of the lane
      road_border_distance_m: 15.0 # [m] road_borders within this distance of the reference_path become hard boundaries
      close_walkway_gap: true # close the gap the sidewalk leaves between the road_borders where the reference_path crosses a walkway
