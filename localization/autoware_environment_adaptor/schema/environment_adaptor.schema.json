{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Environment Adaptor Node Parameters",
  "type": "object",
  "definitions": {
    "environment_adaptor": {
      "type": "object",
      "properties": {
        "default_environment_id": {
          "type": "integer",
          "default": 0,
          "description": "Environment ID used when the pose is outside all degenerate_area polygons, or when map is not ready"
        },
        "default_longitudinal_scale_factor": {
          "type": "number",
          "default": 1.0,
          "description": "Longitudinal velocity scale factor when no environment match is found. Applied as v_out = v_in * factor"
        },
        "map_longitudinal_scale_factor_attribute": {
          "type": "string",
          "default": "longitudinal_scale_factor",
          "description": "Lanelet2 polygon attribute name for per-area longitudinal scale factor override"
        },
        "default_output_pose_covariance": {
          "type": "array",
          "items": { "type": "number" },
          "minItems": 36,
          "maxItems": 36,
          "description": "Default 6x6 pose covariance (row-major, 36 elements) in body frame, used when no environment-specific covariance is defined"
        }
      },
      "patternProperties": {
        "^area_subtype_[a-zA-Z0-9_]+$": {
          "type": "object",
          "properties": {
            "environment_id": {
              "type": "integer",
              "description": "Environment ID assigned to Lanelet2 polygons with matching subtype attribute"
            }
          },
          "required": ["environment_id"],
          "additionalProperties": false
        },
        "^environment_[0-9]+_output_pose_covariance$": {
          "type": "array",
          "items": { "type": "number" },
          "minItems": 36,
          "maxItems": 36,
          "description": "6x6 pose covariance (row-major, 36 elements) in body frame for the specified environment ID"
        },
        "^environment_[0-9]+_longitudinal_scale_factor$": {
          "type": "number",
          "description": "Longitudinal velocity scale factor for the specified environment ID, used when the map polygon has no override attribute"
        }
      },
      "required": [
        "default_environment_id",
        "default_longitudinal_scale_factor",
        "map_longitudinal_scale_factor_attribute"
      ],
      "additionalProperties": false
    }
  },
  "properties": {
    "/**": {
      "type": "object",
      "properties": {
        "ros__parameters": {
          "$ref": "#/definitions/environment_adaptor"
        }
      },
      "required": ["ros__parameters"],
      "additionalProperties": false
    }
  },
  "required": ["/**"],
  "additionalProperties": false
}
